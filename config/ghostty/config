# This is the configuration file for Ghostty.
#
# Run `ghostty +show-config --default --docs` to view a list of
# all available config options and their default values.
#
# Ghostty can reload the configuration with:
# (default: Command + Shift + comma on macOS and (Control + Shift + comma on other platforms).
# Some config only only apply with a full restart.
#
# TIP: blur goes nice with a blur my shell setup

# Shell
shell-integration = zsh
command = "/bin/zsh"

# Font
#font-family = BlexMono Nerd Font Mono
font-family = Maple Mono Normal NL NF
font-family-bold = Maple Mono Normal NL NF Bold
font-family-italic = Maple Mono Normal NL NF Italic
font-family-bold-italic = Maple Mono Normal NL NF Italic

font-size = 15

# Background
background-opacity = 0.8
background-blur = true

# Cursor
cursor-style = bar

# Theme
theme = Monokai DarkPlus
shell-integration-features = no-cursor

# Window size
window-height = 28
window-width = 100

# Keybinds
keybind = ctrl+alt+h=goto_split:left
keybind = ctrl+alt+l=goto_split:right
keybind = ctrl+alt+k=goto_split:up
keybind = ctrl+alt+j=goto_split:down

# Scrolling
scrollback-limit = 100000

# Mouse
mouse-hide-while-typing
